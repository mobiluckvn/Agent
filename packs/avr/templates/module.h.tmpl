/* {purpose}
 *
 * {status}
 *
 * Tệp này do `eaa interface propose {module}` sinh từ khuôn của Platform Pack.
 * Nó là HỢP ĐỒNG GỌI: mỗi hàm nói rõ có gọi được trong ngắt không, có chặn
 * không, có tái nhập được không. Ba câu ấy một chữ ký hàm không trả lời được,
 * mà người viết mã nhúng buộc phải biết trước khi gọi.
 */

#ifndef {guard}
#define {guard}

#include <stdint.h>
#include <stdbool.h>
{includes}

{declarations}

#endif /* {guard} */
