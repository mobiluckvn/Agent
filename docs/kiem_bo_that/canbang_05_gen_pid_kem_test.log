
Vòng lặp chuẩn — logic_pid
──────────────────────────
── sinh lần đầu ──
  compile: ĐẠT (0 lỗi, 0 cảnh báo, 0.07s)
  size: ĐẠT (0 lỗi, 0 cảnh báo, 0.01s)
  static: ĐẠT (0 lỗi, 0 cảnh báo, 0.00s)
  unittests: KHÔNG ĐẠT (1 lỗi, 0 cảnh báo, 0.00s)
      Không có bộ kiểm thử đơn vị nào trong /Users/v/Documents/KTDT/projects/robot_balance/tests. Cổng này KHÔNG đạt khi chưa có test — 'chưa có gì để chạy' không phải là 'đã kiểm chứng'.

Đây KHÔNG PHẢI LỖI MÃ của module vừa sinh: thứ còn thiếu là một phần của DỰ ÁN, không nằm trong tệp đang sửa. Không bản vá nào của module làm cổng này đạt được.

Bộ kiểm chạy bằng PYTEST trên máy chủ: đặt tệp `test_*.py` vào /Users/v/Documents/KTDT/projects/robot_balance/tests. Firmware được viết tách lớp trừu tượng phần cứng chính là để chạy được ở đây (công đoạn C2) — test là mã PYTHON gọi vào lớp giả lập, không phải mã C.

Lỗi CẤU HÌNH, không phải lỗi mã — vòng tự sửa không mở.
Không có bộ kiểm thử đơn vị nào trong /Users/v/Documents/KTDT/projects/robot_balance/tests. Cổng này KHÔNG đạt khi chưa có test — 'chưa có gì để chạy' không phải là 'đã kiểm chứng'.

Đây KHÔNG PHẢI LỖI MÃ của module vừa sinh: thứ còn thiếu là một phần của DỰ ÁN, không nằm trong tệp đang sửa. Không bản vá nào của module làm cổng này đạt được.

Bộ kiểm chạy bằng PYTEST trên máy chủ: đặt tệp `test_*.py` vào /Users/v/Documents/KTDT/projects/robot_balance/tests. Firmware được viết tách lớp trừu tượng phần cứng chính là để chạy được ở đây (công đoạn C2) — test là mã PYTHON gọi vào lớp giả lập, không phải mã C.
