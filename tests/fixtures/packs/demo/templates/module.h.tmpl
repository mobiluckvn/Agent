/* {purpose}
 *
 * {status}
 *
 * Khuôn tiêu đề của pack giả lập — giữ đúng hình dạng khuôn thật để phép kiểm
 * ở TC-56 kiểm được cơ chế, không kiểm một bản rút gọn khác hẳn.
 */

#ifndef {guard}
#define {guard}

#include <stdint.h>
{includes}

{declarations}

#endif /* {guard} */
